# AI-Footfall-Counter
A Python-based project that uses YOLOv8 and OpenCV to detect and count people entering and exiting through a specific area in real-time. The system tracks each person’s movement across frames, defines a virtual counting line, and displays entry/exit counts dynamically.
